import React, { FC, memo } from 'react';

interface IContentOptions {
   //
}

const ReferralList: FC<IContentOptions> = memo((props: IContentOptions) => {
   return <div>Тут будет список назначений</div>;
});

export default ReferralList;
